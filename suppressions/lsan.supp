# Likely false positives
leak:PyMem_RawCalloc
leak:PyMem_RawMalloc

# Reported leaks in Fontconfig are likely false positives due to
# missing instrumentation (i.e. it's build without ASan/LSan).
leak:libfontconfig.so.1

# Freed on exit, see:
# https://gcc.gnu.org/bugzilla/show_bug.cgi?id=36298#c1
leak:GOMP_parallel
leak:___kmp_allocate
